; PHP-FPM picks this up automatically from the public/ directory.
; Used so long-running endpoints (Excel import, bulk data-delete) don't get killed by PHP.
max_execution_time = 0
max_input_time = 3600
upload_max_filesize = 50M
post_max_size = 60M
memory_limit = 1024M
