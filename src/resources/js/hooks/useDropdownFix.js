/**
 * import useDropdownFix from "./../hooks/useDropdownFix";
 * useDropdownFix();
 */
import { useEffect } from "react";
import "./useDataTableDropdownFix.css";

export default function useDropdownFix() {
    useEffect(() => {
        // nothing needed – just importing CSS fixes the issue
    }, []);
}